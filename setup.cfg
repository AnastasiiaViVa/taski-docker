[flake8]
# Игнорируем правила W503 и F811
ignore =
    W503,
    F811

# Не проверяем служебные директории
exclude =
    tests/,
    */migrations/,
    venv/,
    */venv/,
    env/,
    */env/,

# В settings.py разрешаем длинные строки
per-file-ignores =
    */settings.py:E501
